import 'package:flutter/material.dart';

class RowsColumns extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('RowsColumns'),
      ),
      body: Container(),
    );
  }
}
